# Image assets

Drop README/diagram images in this folder.

| File | Used by | What it should show |
| --- | --- | --- |
| `happyrobot-workflow.png` | Root `README.md` (Architecture section) | The HappyRobot voice-agent workflow diagram (Web call → Inbound Voice Agent → Prompt → tools → Classify → Extract → Record call). |

> Save the workflow screenshot you exported from HappyRobot here as **`happyrobot-workflow.png`** so it renders in the main README. You can add dashboard screenshots here too (e.g. `dashboard-kpis.png`) and reference them the same way.
